{
  "name": "medconduta-ai-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "login": "wrangler login",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put GEMINI_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
